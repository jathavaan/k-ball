import { ScrollSectionProps } from './scrollSection.types';
import { StyledScrollSection } from './scrollSection.style';


export const ScrollSection = (props: ScrollSectionProps) => {
    return <StyledScrollSection {...props}></StyledScrollSection>
}

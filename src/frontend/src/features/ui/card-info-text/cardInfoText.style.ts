import { styled, Typography } from "@mui/material";

export const StyledContentText = styled(Typography)(({ theme }) => ({
  size: "1.4rem",
  fontWeight: 800,
}));

export const StyledTitleText = styled(Typography)(({ theme }) => ({
  size: "1rem",
  fontWeight: 300,
}));
